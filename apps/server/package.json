{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test:rate-limit": "tsx test-rate-limit.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.932.0",
    "@aws-sdk/s3-request-presigner": "^3.932.0",
    "@hono/node-server": "^1.14.4",
    "@krypt-vault/auth": "workspace:*",
    "@krypt-vault/db": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "^4.8.2",
    "hono-rate-limiter": "^0.4.2",
    "libsodium-wrappers": "^0.7.15",
    "uuid": "^13.0.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/libsodium-wrappers": "^0.7.14",
    "@types/node": "catalog:",
    "tsdown": "catalog:",
    "tsx": "^4.19.2",
    "typescript": "catalog:"
  }
}
